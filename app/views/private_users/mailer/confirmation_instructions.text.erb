Hello <%= @email %>!

You can confirm your Oneslate private instance account email through the link below:

<%= confirmation_url(@resource, confirmation_token: @token, host: Thread.current[:request_host]) %>
